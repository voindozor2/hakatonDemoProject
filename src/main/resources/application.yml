server:
  port: 8088
  servlet:
    context-path: /hakatonDemo

spring:
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false
    datasource:
      driver-class-name: org.postgresql.Driver
    application:
      name: hakatonDemo
  datasource:
    driver-class-name: org.postgresql.Driver
    username: PotatoHD
    password: password
    url: jdbc:postgresql://localhost/java

rabbit:
  host: localhost
  port: 5672